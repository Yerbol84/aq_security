name: server_data
description: AQ Security Data Layer (Isolated Vault Server)
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.3.0

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dart_vault:
    path: ../../../dart_vault_package
  aq_schema:
    path: ../../aq_schema

dev_dependencies:
  lints: ^3.0.0
